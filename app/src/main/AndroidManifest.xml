<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app.weather">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".signup" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".reset_password" />
        <activity android:name=".rview" />
        <activity android:name=".rviewadapter" />
        <activity android:name=".Switcher" />
        <activity android:name=".RainfallView" />
        <activity android:name=".MeanTempView" />
        <activity android:name=".MaxTempView" />
        <activity android:name=".MinTempView" />
        <activity android:name=".RainfallPred" />
        <activity android:name=".MeanTempPred" />
        <activity android:name=".MinTempPred" />
        <activity android:name=".MaxTempPred" />
        <activity android:name=".CurrentTemp"></activity>
    </application>

</manifest>